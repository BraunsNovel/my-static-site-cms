{
  "name": "brauns-novel",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js && cp -r src/. dist/",
    "dev": "node scripts/build.js && npx serve src"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
